<%!
  from utils import locstr,date_str
  import SalusDevice
%>

<table class="table">
  <thead>
    <tr>
      <th scope="col">        
        <a class="" href="index.php?page=home&cmd=edit_device&device_id=<?php echo $device->id?>">${device.name}></a>            
      </th>
      <th scope="col">${locstr("Curr Temp")}></th>
      <th scope="col">${locstr("Curr Mode")}></th>      
    </tr>
  </thead>
	<tbody>
    % if device.status==SalusDevice.STATUS_OFFLINE:
      <tr><td>
          <div style="color:red">${locstr('Offline')}</div>
      </td></tr>
    % else:
      ${zones_html}
    % endif
	</tbody>
</table>
